# Core FastAPI and async dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0

# Lightweight LLM and embeddings
transformers==4.44.2
# Note: Install PyTorch with CUDA support for GPU usage:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or for CPU only: pip install torch torchvision torchaudio
torch>=2.0.0
sentence-transformers==3.0.1
accelerate>=0.33.0

# RAG and vector database - Updated to compatible versions
llama-index==0.10.55
llama-index-embeddings-huggingface==0.2.2
# Remove the conflicting package and use a compatible alternative
# llama-index-llms-huggingface==0.1.4  # This causes the conflict

# HTTP client for async requests
httpx==0.27.0

# Monitoring and logging
structlog==24.1.0
prometheus-client==0.20.0

# Development and testing
pytest==8.3.2
pytest-asyncio==0.23.6
black==24.4.2
isort==5.13.2
flake8==7.0.0

# Streamlit client
streamlit==1.39.0